# 10. 联机（M9）设计

> 状态：**设计已定稿 + 执行计划已确认；P0 网络地基 / P1 移动同步 / P2-1 武器权威化已完成，
> P2-2 AI 搬服务器进行中（见第 20 节）**。
> 决策依据：ADR-001（服务端权威 + NGO 选型）、决策 D-08 / D-24 / D-26 / D-27 / D-28 / D-29；
> 外网接入方式待确认（见第 14 节 Q-07）。

---

## 1. 目标与范围

| 项目 | 结论 |
| --- | --- |
| 玩法 | **2~4 人 PVE 合作**，共同对抗 AI；单机 1 人 |
| 拓扑 | **统一客户端 + 独立专用服务器**；不做 Host-Client |
| 客户端形态 | **同一个客户端程序**：主菜单含「联机」入口，单机与联机不做版本区分 |
| 单机模式 | **本机内嵌服务器**（进程内跑同一套权威逻辑），与联机共用一条代码路径 |
| 平台 | 客户端仅 Windows（D-15）；服务器本机 / 局域网 / 云主机 |
| **不做** | PVP、匹配系统、反作弊、完整账号体系、跨平台联机、服务器集群、多房间并发 |

## 2. 形态：一套代码，两种运行方式

```text
[客户端 RaidDemo.exe]                 [服务器 RaidDemoServer.exe（无头）]
  主菜单                               监听端口（默认 7777，可配置）
   ├─ 单机：进程内启动内嵌服务器          ├─ 房间生命周期管理（单房间）
   └─ 联机：连接远端/本机服务器            ├─ 权威模拟（移动 / 战斗 / AI / 战局）
        ├─ 创建房间（成为房主）            ├─ 服务端存档（按账号隔离）
        └─ 加入房间（输地址 + 密码）        └─ 版本握手（对接 M10 热更）
```

- 逻辑与客户端内嵌服务器**是同一套代码**，服务器程序有两种外壳：
  - **本机 / 局域网**（Windows）：**带可视化 Dashboard 的服务器窗口**——显示监听地址、房间状态、玩家列表、日志，
    并提供开局 / 停止房间等操作；
  - **云主机**（Linux）：`-batchmode -nographics` 无头运行，通过 **Web 状态页**查看房间、玩家与日志
    （服务器内置 HTTP 状态页；备选：宝塔静态页 + `status.json`，Q-08 已确认）；
- 启动参数通过命令行传入，**不含绝对路径**（第 3 章约束）；
- 服务器启动后显示两行地址：`本机 127.0.0.1:7777` 与 `局域网 <内网IP>:7777`（云主机显示公网地址），方便房主转达。

## 3. 权威边界

| 内容 | 归属 |
| --- | --- |
| 位置 / 朝向、开火与命中、伤害与死亡、AI 行为、掉落、容器与搜刮进度、撤离计时、战局结算 | **服务器权威** |
| 输入采集、预测、插值、动画 / 特效 / 音效 / UI | 客户端 |
| 仓库、金币、任务等局外进度（联机模式） | **服务端存档**，按账号隔离（见第 6 节） |
| 仓库、金币、任务等局外进度（单机模式） | 本地存档，**与联机存档互不兼容** |

## 4. 房间模型：一台服务器 = 一个房间

```text
服务器启动（空闲）
   │  第一个客户端点「创建房间」
   ▼
房间成立 —— 创建者成为房主（房间管理员）
   │  其他人「加入房间」（地址 + 密码）
   ▼
等待大厅（2~4 人，房主可见全员列表）
   │  房主点「开始战局」
   ▼
战局（服务器权威）→ 撤离 / 阵亡 → 结算 → 回到大厅
   │  房主「解散房间」
   ▼
服务器回到空闲，等待下一批人创建
```

- **房主只是管理员**：负责开局、设置（房间名 / 密码 / 地图 / 难度）与踢人；
  他没有任何规则裁判权——权威始终在服务器，房主掉线不影响战局。这是与 Host-Client 的本质区别。
- **多房间并发明确推迟**：多房间要求服务器进程内并发多个会话实例（每个会话独立的战斗世界、AI、事件总线、存档区），
  对作品集 Demo 收益有限、成本接近翻倍。

## 5. 连接与加入

| 场景 | 做法 |
| --- | --- |
| 本机（服务器与客户端同机） | 一键「本机联机」，自动填 `127.0.0.1:7777` |
| 局域网 | **UDP 广播自动发现**：客户端扫描后列出局域网房间，免手输 IP；同时保留手输兜底 |
| 云主机 / 外网 | 手输公网地址（阿里云轻量 `47.104.210.207:7777`，公网直连）；客户端与启动器保留"本机 / 云主机 / 自定义"服务器档案，可一键切换 |
| 密码 | 可选 4 位数字，房主设置；密码错误有明确提示 |

**不做「房间号」**：房间号需要目录服务器把号码翻译成地址，等于额外维护一套后端；
密码已经能承担"防止乱入"的职责。若将来出现目录服务需求，再评估。

## 6. 身份与存档（联机模式）

### 6.1 轻量账号

- 首次进入联机模式：输入**昵称 + 4~6 位口令** → 服务器建号（口令存哈希），返回 token 存本地；
- 之后自动登录；换机器时用昵称 + 口令重新登录；
- **不做**邮箱验证、找回密码、改密流程——那是产品功能，不是岗位考察点。

### 6.2 服务端存档

- 存储位置：服务器侧 `server_saves/<账号>.json`，按账号隔离；
- 内容：仓库、金币、任务进度、联机节奏数据；
- **与单机存档完全隔离、互不兼容**——这是设计目标而非妥协：避免"联机刷装备带回单机"的平衡问题；
- 经济循环归服务器：商人与仓库操作在客户端产生指令、服务器落库，客户端只做界面与即时反馈。

## 7. 同步机制（沿用 ADR-001 的状态同步）

| 内容 | 机制 |
| --- | --- |
| 位置 / 朝向 / 血量 / 弹药 / 战局倒计时 | 快照（20 Hz）+ 客户端插值 |
| 开火 / 命中 / 拾取 / 换弹 / 死亡 / 撤离 | 事件 RPC（一次性） |
| 本地玩家移动 | 客户端**预测** + 服务器**和解**（不做完整战斗回滚） |
| 远端玩家与 AI | 客户端插值 + 外推 |
| 战斗判定 | **表现先行、服务器裁定**：客户端立即播弹道 / 火光 / 音效，命中与伤害以服务器结果为准 |
| AI | 全部在服务器运行，客户端只播动画与特效 |
| 版本握手 | 客户端上报"本体版本 + Addressables catalog 哈希 + 热更程序集版本"，不一致则提示更新（对接 M10） |

## 8. 网络消息（在 00 文档 7.9 草案上扩展）

| 消息 | 方向 | 内容 |
| --- | --- | --- |
| `LoginRequest` / `LoginResult` | C→S / S→C | 昵称 + 口令 → token / 拒绝原因 |
| `CreateRoom` / `JoinRoom` | C→S | 房间名、密码 / 服务器地址已由连接隐含 |
| `RoomState` | S→C | 成员列表、房主标识、准备状态 |
| `StartRaid` | C→S（仅房主） | 开始战局 |
| `PlayerInputCommand` | C→S | 输入位、朝向、时间戳、序号 |
| `PlayerStateSnapshot` | S→C | 位置、朝向、血量、武器状态 |
| `FireCommand` / `FireResult` | C→S / S→C | 开火请求 / 命中结算结果 |
| `InventoryCommand` | C→S | 拾取、丢弃、移动、装填 |
| `RaidStateSync` | S→C | 战局阶段、剩余时间、撤离点状态 |

## 9. 批次计划（每批可玩、可验收、测试全绿）

| 批次 | 内容 | 验收判据 |
| --- | --- | --- |
| **P0 网络地基** | NGO 2.13.2 接入；服务器 / 客户端启动分流；**单机改为"本机内嵌服务器"**；会话装配从 `SceneBootstrap` 中拆出 | 单机全流程（安全屋 → 战局 → 撤离）在新权威模型下可玩；服务器能无头启动并打印监听地址；EditMode 全绿 |
| **P1 移动同步** | 输入上行 + 快照下行 + 本地预测与和解 + 远端插值 | 2 个客户端同局，互相看到平滑移动、朝向与动画正确；本地操作无明显延迟感 |
| **P2 战斗与 AI** | 开火 / 换弹 / 命中 / 伤害 / 死亡同步；AI 全部搬到服务器 | 2 人共同对抗 AI，弹药、伤害、死亡表现一致；AI 在客户端只做表现 |
| **P3 交互与战局闭环** | 容器、拾取、装备、背包操作、撤离、结算 | 两人抢同一容器有确定结果；撤离 / 阵亡各自结算；战局可重开 |
| **P3.5 倒地与救援** | 生命系统区分「失能（倒地待救）」与「死亡」两态；队友可施救扶起，无人施救到时限转死亡 | 两人同局：一人被打倒进入失能，队友救起后可继续作战；超时无人救则按死亡结算；单机 1 人保持现语义（倒地即死亡） |
| **P4 大厅与房间 + 轻量身份** | 主菜单联机入口、创建 / 加入房间、等待大厅、房主开局、密码、局域网发现；昵称 + 口令登录；**服务器 Dashboard**（本机版：地址、房间状态、玩家列表、日志、停止房间） | 本机 3 进程（服务器 + 2 客户端）跑通"创建 → 加入 → 开局"；局域网两台电脑跑通；Dashboard 能实时看到房间与玩家；错误口令 / 重复昵称有明确提示 |
| **P5 服务端存档与经济** | 仓库 / 金币 / 任务搬到服务器；撤离结算落库；与单机存档隔离 | 两个账号的进度互相独立；单机存档不受联机影响；服务器重启后进度仍在 |
| **P6 部署与收尾** | Linux 服务器构建（Dedicated Server）、**云端 Web 状态页**、一键启动脚本、云主机部署说明（阿里云轻量：公网 IP 直连）、断线处理、全量回归 | 云主机跑通；浏览器能查看房间 / 玩家 / 日志；短线重连可回局；README 给出可复现步骤 |

> **范围确认（2026-09-13 负责人拍板）**：
> - **P-07「倒地与救援」纳入 M9**（即上表 P3.5）——该机制在单机 1 人下没有玩法意义，联机后才成立；
> - **P-16「AI 之间避让与编队」与 D-10「AI 小队协同」推迟出 M9**——M9 的重心是把 AI 从客户端搬进服务器，
>   同时在网络层与行为层动手会让故障定位变难；避让实现成本低（只需替换 `AiMovement` 内部实现），
>   若 P6 收尾时时间富余可作为加练补做。

## 10. 关键改造清单（对现有代码的影响）

| 现有实现 | 改造 |
| --- | --- |
| `SceneBootstrap`（单例组装根） | 拆出"会话装配"，支持"进程内一个会话"与"服务器进程内一个会话"两种宿主 |
| `EventBus` / `ServiceLocator` | 保持接口不变，改为会话级生命周期（服务器进程同样持有） |
| `PhysicsHitProbe`（现住表现层） | 服务端实现同一接口（无头环境同样有 PhysX），表现层保留原实现 |
| `Meta` / `SaveFileStore`（本地存档） | 抽出存储接口；联机模式换为服务器实现（按账号），单机保持本地文件 |
| `CommandRouter`（M0 预留） | 联机时命令来自网络输入，单机时来自本地输入——输入源可替换 |
| UI | 新增主菜单联机入口、登录界面、房间 / 大厅界面 |
| 构建流程 | 新增 Dedicated Server 构建目标与一键启动脚本 |

## 11. 风险与降级策略

1. **预测 / 和解是最大复杂度来源** → 先做"无预测也能玩"（接受延迟抖动），再加本地玩家预测；时间不足时只保留移动预测。
2. **`SceneBootstrap` 单例是最大改造点** → P0 先做装配审计，把"服务器必须能跑哪些代码"圈出来。
3. **无头服务器上的 NavMesh 与物理** → P2 实测；导航烘焙数据需随服务器包一起发布。
4. **云主机形态**：已确定为阿里云轻量 **Linux**（Q-07 已解决）→ 需要安装 Unity 的 Linux Dedicated Server 构建模块；
   首次部署先验证镜像运行库（glibc 版本），过旧则换 Ubuntu 22.04 镜像。
5. **断线重连** → 目标：短线（60 秒内）可回局，超时按阵亡结算；可降级为"断线即回大厅"。

## 12. 验收与演示脚本

| 场景 | 步骤 |
| --- | --- |
| 本机（最常用） | 起 `RaidDemoServer.exe` → 起 2 个客户端 → 分别"创建房间 / 加入房间" → 同局撤离 |
| 局域网 | 服务器跑在一台电脑，另一台用自动发现或手输内网 IP 加入 |
| 云主机 | 部署到阿里云轻量（Linux，公网 IP 直连 `47.104.210.207:7777`），客户端 / 启动器填公网地址；服务器状态用 Web 状态页查看 |

> **验收环境变更（2026-09-13）**：现场只有一台 Windows 电脑，上表"局域网"一行降级为
> **「本机双进程 + 云主机外网」**；UDP 广播自动发现仍要实现（代码与测试照做），
> 实机双机验收推迟到有第二台电脑时补测。

## 13. 面试可讲点

- 为什么必须服务端权威、为什么不做 Host-Client；
- 状态同步 vs 帧同步的取舍；快照频率与带宽预算；
- 客户端预测与和解的边界（为什么战斗不做完整回滚）；
- AI 在服务器运行的代价与收益；
- 版本握手如何保证联机双方资源 / 代码一致（与 M10 热更衔接）。

## 14. 待确认

| 编号 | 事项 | 待确认内容 | 截止 |
| --- | --- | --- | --- |
| Q-07 | 联机对外接入与云主机形态 | **已确认（2026-09-13）**：阿里云轻量应用服务器（华北1 青岛，Linux 宝塔镜像），**公网 IP 直连**；启动器更新源可指向本机或云主机 | 已解决 |
| Q-08 | 云端服务器的可视化形式 | **已确认（2026-09-13）：Web 状态页**（服务器内置 HTTP 状态页；备选：宝塔静态页 + `status.json`）；不换 Windows Server 镜像 | 已解决 |
| Q-09 | 云防火墙的 UDP 规则 | 7777 目前只有 TCP 规则，**需补 UDP**（游戏同步走 UDP）；未完成前云主机连不上 | 待负责人操作；**不阻塞 P0~P5**（本机推进） |
| Q-10 | 局域网验收环境 | 现场只有一台 Windows 电脑 → 局域网验收降级为「本机双进程 + 云主机外网」 | 已确认（2026-09-13） |

---

## 15. 部署环境与接入（2026-09-13 体检结论）

> 完整接入信息（公网 IP、SSH 命令、面板地址）记在**不入库**的 `Docs/Local/云主机接入.local.md`，
> 本文件只保留不含敏感信息的结论。理由：本仓库是**公开仓库**，公网 IP 入库等于对外公布入口。

### 15.1 云主机（阿里云轻量 · 华北1 青岛）

| 项目 | 实测值 | 结论 |
| --- | --- | --- |
| 系统 | Alibaba Cloud Linux 3.2104 U11（RHEL 8 系），内核 5.10.134 | 合格 |
| glibc | 2.32 | 满足 Unity Linux 构建 ≥ 2.17 的运行要求 |
| 硬件 | 2 核 / 1871 MB 内存 / 磁盘 40 GB（可用 31 GB） | 单房间 2~4 人够用 |
| 工具链 | tar、unzip、curl、wget、python3 已装；`git` 未装（需要时再装） | — |
| 面板 | 宝塔面板独占 8888/tcp；未装 nginx / mysql | Web 状态页用服务器内置 HTTP 实现，不依赖面板 |
| 系统防火墙 | firewalld 已放行 `7777/udp`（本次补）与 `8080/tcp` | 合格 |
| 云防火墙 | 7777 只有 TCP 规则，**UDP 待补** | 见 15.3 与 Q-09 |

### 15.2 端口规划

| 端口 | 协议 | 用途 | 对外暴露 |
| --- | --- | --- | --- |
| 7777 | UDP | 游戏同步（Netcode for GameObjects / Unity Transport） | 是（玩家直连） |
| 8080 | TCP | 服务器 Web 状态页 | 是（只读状态） |
| 8888 | TCP | 宝塔面板 | **仅运维**，建议限制来源 IP |
| 22 | TCP | SSH（**仅公钥登录**，`PasswordAuthentication no`） | 仅运维 |

### 15.3 连通性实测（从开发机出网，2026-09-13）

| 目标 | 方法 | 结果 |
| --- | --- | --- |
| SSH | 公钥登录（私钥在开发机，不入库） | **通**；服务器保持关闭密码登录 |
| TCP 8080 | 开发机连接 + 服务器侧记录来源 | **通**（服务器记录到开发机来源 IP，链路完整） |
| UDP 7777 | 两轮共 6 个自定义 UDP 包 | **不通**：服务器未收到任何包；同机 DNS over UDP 正常，排除开发机出网问题 → 判定为云防火墙缺 UDP 规则 |

> 影响范围：仅 **P6 云主机验收**受阻；P0~P5 在本机用「服务器进程 + 2 客户端」推进，不等这条规则。

---

## 16. 开工前准备清单

| 项 | 归属 | 状态 |
| --- | --- | --- |
| 云防火墙补 **UDP 7777** | 负责人（阿里云控制台 → 防火墙） | 待办，不阻塞 P0~P5 |
| Unity 补装 **Linux 构建模块（Dedicated Server）** | 负责人（Unity Hub） | 待办，**P6 前必须完成**；当前编辑器只装了 Windows + WebGL |
| 局域网验收环境（两台 Windows 电脑） | 负责人 | 当前只有一台 → 已按降级方案记录（Q-10） |
| 服务器 SSH 公钥接入 | 已完成（2026-09-13） | 开发机持有私钥，**不入库** |
| 宝塔面板密码轮换 | 负责人 | 建议 M9 / M10 收尾后轮换一次（调试期曾以明文交换） |

---

## 17. P0 交付记录（2026-09-13）

### 17.1 做了什么

| 交付物 | 位置 | 说明 |
| --- | --- | --- |
| 会话作用域 | `Bootstrap/Session/SessionScope.cs` | 事件总线 / 服务定位器 / 命令路由 / 日志的创建与释放；单机与服务器共用同一套 |
| 服务器启动参数 | `Bootstrap/Session/ServerLaunchOptions.cs` | 解析 `-server` / `-port` / `-room` / `-saveDir` / `-logLevel`；存档目录只接受相对路径 |
| 地址报告 | `Bootstrap/Session/ServerAddressReporter.cs` | 「本机 + 局域网（有网关的网卡优先）+ 云主机提示」，取不到网卡也不影响启动 |
| 服务器运行时 | `Bootstrap/Session/ServerRuntime.cs` | 建会话、起 NGO 监听（60 Hz Tick、关闭场景管理）、心跳日志、干净退出 |
| 进程入口 | `Bootstrap/Session/ServerEntryPoint.cs` | `BeforeSceneLoad` 判定角色、`AfterSceneLoad` 起服务器；`ServerMode.IsActive` 供两个装配根自查 |
| 服务器构建 | `Bootstrap/Editor/ServerBuildMenu.cs` | 菜单 `RaidDemo/M9/构建专用服务器（Windows）`，构建后自动还原子目标 |
| 装配改造 | `SceneBootstrap.cs` / `SafeHouseBootstrap.cs` | 服务创建改为会话化；服务器模式下客户端装配自行退出 |

### 17.2 验收证据

| 验收点 | 证据 |
| --- | --- |
| 单机全流程在新权威模型下可玩 | EditMode **453 / 453** 全绿（原 432 + 新增 21）；实机播放模式确认两个装配根均正常建立会话——战局侧：命令路由 / 移动模拟 / 容器注册 / 背包界面齐备；安全屋侧：装配存在 + 3 项会话服务 |
| 服务器能无头启动并打印监听地址 | 出 Windows 包（161 MB / 43 秒 / 0 错误）后以 `-server -batchmode -nographics -port 7777` 启动：进程存活、**UDP 7777 监听在 `0.0.0.0`**、日志打印「本机 127.0.0.1:7777 / 局域网 …:7777 / 云主机请填公网 IP」 |
| 会话边界可回归 | `SessionScopeTests`（7 条：服务解析、静态入口清空、重复释放、新旧会话共存）、`ServerLaunchOptionsTests`（12 条：默认值、非法端口、绝对路径、超长房间名、未知参数忽略）、`ServerAddressReporterTests`（2 条） |

> 单机全流程的「安全屋 → 战局 → 撤离」由项目负责人在编辑器里实机走一遍作为最终确认；
> 上述证据证明的是「装配与权威边界改造后各系统仍被正确建立」。

### 17.3 P0 暴露的工具链限制（详见 M9 排障手册）

- **Unity CLI 的 PlayMode 运行器不可用**（`services-gateway` 报错，返回 0 条）→ 验收改用真实出包；
- **Dedicated Server 构建模块未安装**（Windows / Linux 都是独立模块）→ P6 前补装，见第 16 节；
- 构建会往 `Assets/Resources` 写性能采样文件 → 已加入 `.gitignore`。

---

## 18. P1 交付记录（2026-09-13）

### 18.1 做了什么

| 交付物 | 位置 | 说明 |
| --- | --- | --- |
| 预测与和解内核 | `Modules/Simulation/Player/MovementPredictionBuffer.cs` | 记录每次发出的输入与预测结果，按序号对账，超容差（默认 8 cm）才回滚重放 |
| 远端插值 | `Modules/Simulation/Player/MovementInterpolationBuffer.cs` | 20 Hz 快照 → 每帧平滑位置，渲染延迟 100 ms，两端钳制 |
| 服务器权威世界 | `Modules/Simulation/Player/MovementServerWorld.cs` | 玩家增删、输入序号校验、60 Hz 固定步长、20 Hz 快照节拍、单步上限 |
| 全量快照 | `Modules/Simulation/Player/PlayerMovementSnapshot.cs` | 含体力恢复计时器——回滚起点必须完整，否则体力曲线对不上 |
| 网络层 | `Bootstrap/Session/Network/` | 消息定义、命名消息通道、`NetworkConfigFactory`、服务器权威接线 |
| 客户端联机 | `Bootstrap/SceneBootstrap.Multiplayer*.cs` | 固定步预测、输入上行、对账回滚、远端玩家插值表现 |
| 远端表现 | `Modules/Presentation/Player/RemotePlayerView.cs` | 复用角色模型与动画参数规则，与本地玩家的表现一致 |
| 启动与调试入口 | `LaunchOptions` / `ClientPlayMenu` | `-connect` / `-map` / `-autowalk`；编辑器菜单「以联机客户端进入」 |

### 18.2 验收证据（2026-09-13，本机三进程）

```
服务器：玩家 1 已加入（在线 1 人）。
服务器：玩家 2 已加入（在线 2 人）。
服务器：心跳：房间「默认房间」在线 2 人。

客户端 A（玩家 1）：[联机] 已连接，玩家标识 1。
客户端 A：[联机] 远端玩家 2 已进入视野。｜ 位置 (3.26, 0.00)
客户端 A：[联机] 回滚重放：误差 5.542 米，重放 0 条输入（seq=4）→ 随后误差收敛到 0.117 米

客户端 B（玩家 2）：[联机] 已连接，玩家标识 2。
客户端 B：[联机] 远端玩家 1 已进入视野。｜ 位置 (12.77, -2.48) → (19.28, -3.83) → (26.13, -5.26)
```

**结论**：2 个客户端同局、服务器为权威、客户端互相看到远端玩家**连续移动**（位置随时间变化且平滑），
预测与和解链路工作正常（回滚日志显示误差被纠正且收敛）。

> 首次对账会出现一次较大误差（客户端本地出生点与服务器派发点不同），随后收敛。
> 更平滑的做法是连接后用首个快照直接吸附一次，作为 P2 的小优化。

### 18.3 P1 暴露的问题（详见 M9 排障手册）

- **主菜单把 `timeScale` 冻住导致网络栈停摆**（P-09）——联机客户端连上即为战局状态；
- **NGO 在非开发版构建里不打日志**（P-10）——诊断联机要先确认日志还在；
- **`NetworkConfig` 两端不一致会被静默拒绝**（P-11）——配置收进 `NetworkConfigFactory` 单一来源。

---

## 19. P2-1 交付记录：玩家武器权威化（2026-09-13）

### 19.1 做了什么

| 交付物 | 位置 | 说明 |
| --- | --- | --- |
| 服务器战斗协调器 | `Bootstrap/Session/Network/ServerCombatCoordinator.cs` | 每名玩家一套武器控制器；弹药、换弹、开火节奏、命中判定、伤害结算全在这里 |
| 战斗消息 | `Bootstrap/Session/Network/CombatNetworkMessages.cs` | 下行四类事件（开火 / 伤害 / 摧毁 / 换弹）合成一条消息 |
| 上行承载 | `MovementNetworkMessages.cs` | 扣扳机与换弹请求搭在移动输入上（同频、同一条时序） |
| 服务器接线 | `ServerRuntime.Combat.cs` | 权威世界 ↔ 网络：消费输入、广播结果、把战斗单位编号翻译回玩家编号 |
| 客户端接线 | `SceneBootstrap.Multiplayer.Combat.cs` | 上报意图；把服务器事件还原成本地事件发布——现有特效 / HUD / 命中反馈因此自动生效 |
| 可被命中 | `ServerRuntime.Movement.cs` | 玩家载体加胶囊碰撞体 + 单位层 + `CombatTargetView` |

### 19.2 验收证据（本机三进程，开发版构建）

```
服务器：收到玩家 1 的首条输入：… 扳机=True …
服务器：玩家 1 开火（命中=False，目标=0）        ← 权威开火 60 次
服务器：命中：玩家 2 → 玩家 1，伤害 25.0，剩余 75.0
服务器：命中：玩家 2 → 玩家 1，伤害 25.0，剩余 50.0
服务器：命中：玩家 2 → 玩家 1，伤害 25.0，剩余 25.0
服务器：命中：玩家 2 → 玩家 1，伤害 25.0，剩余 0.0（已阵亡）

客户端 A：已扣下扳机，开始向服务器上报开火意图
客户端 A：收到战斗事件 60 条（开火 60 / 伤害 4）
```

对应验收标准：

- **弹药由服务器说了算**：弹匣与备弹扣减只发生在服务器侧的战斗协调器里；
- **命中与伤害由服务器裁定**：客户端只上报"我想开枪"与朝向，射线与伤害全在服务器；
- **两端表现一致**：客户端把服务器事件还原成本地事件，弹道与命中反馈走的是与单机相同的表现链路。

### 19.3 本批的已知简化

- 每名玩家按统一规格配发 AK 与 120 发备弹（真实装备来自服务端存档是 P5）；
- 弹药挂 / 背包是**服务端权威容器**，但与客户端背包尚未同步（P3 的交互同步范围）；
- 客户端不做开火预测：按下到出火光有一个往返延迟，按设计文档 §11 的降级策略后置。

---

## 20. P2-2 执行记录：AI 搬服务器（进行中）

### 20.1 目标与拆解

P2-2 要把 AI 从"每个客户端各跑一份"变成"服务器权威 + 客户端只做表现"。拆成四步走：

| 步骤 | 内容 | 状态 |
| --- | --- | --- |
| **20.2 导航地基** | 服务器加载地图后烘焙自己的导航网格，并用真实路径自检 | ✅ 已完成 |
| **20.3 实体高度对齐** | 服务器侧玩家（以及后续 AI）的碰撞载体贴真实地面，而不是固定在 0 米 | ✅ 已完成 |
| **20.4 服务器侧 AI 装配** | AI 调度器、敌人生成、寻路与碰撞、噪音与受击接入服务器会话 | ⬜ 下一步 |
| **20.5 敌人状态下行** | 位置 / 朝向 / 状态 / 开火事件同步给客户端，客户端只插值表现 | ⬜ |

### 20.2 导航地基（2026-09-14）

新增 `Bootstrap/Session/Network/ServerRuntime.Navigation.cs`：地图场景生效后烘焙
`NavMeshSurface`，随后用**服务器自己将要使用的寻路实现**（`NavMeshPathfindingService`）
算三条真实路径并打日志（平地 / 装卸平台高差 / 厂房内部）。

验收证据（Windows 开发版 + `-server -map GreyboxRaid -batchmode -nographics`）：

```
[服务器] 加载地图场景：GreyboxRaid
[服务器] 导航数据已烘焙：三角面顶点 1952 个，耗时 21 ms。
[服务器] 导航自检「堆场平地」：路径可用，3 个途径点。
[服务器] 导航自检「装卸平台高差」：路径可用，2 个途径点。
[服务器] 导航自检「厂房内部」：路径可用，4 个途径点。
```

**结论：无头服务器可以烘焙并使用导航网格。** 关键前提是场景生成器把
`NavMeshSurface.useGeometry` 设为**收集物理碰撞体**（`PhysicsColliders`）而不是渲染网格——
后者要求模型开启 Read/Write，而工程里 93 个模型全部是 `isReadable: 0`。

过程中暴露并修掉了两个真实缺陷（详见 M9 排障手册 P-15 / P-16）：

1. **从 `AfterSceneLoad` 里 `LoadScene` 是延迟生效的**——刚加载完就去找场景里的组件必然找不到。
   改为"登记待办 + 每帧检查活动场景"。
2. **寻路服务写死了"地面在 y=0"**——而 M7 起地图是下沉盆地（谷底 -6 / 平台 -4.8 / 塬面 0），
   于是寻路**全部失败**并静默降级成直线推进。改用与敌人视图、AI 地面高度同一套分层采样
   （`NavMeshGroundSampler`）后恢复正常。
   这条同时解释了此前"AI 在盆地里走路发直"的观感——不是状态机的问题，是寻路从来没成功过。

### 20.3 实体高度对齐（2026-09-14）

同一轮排查发现服务器把玩家碰撞载体放在 `y = 0`，而谷底在 `-6` 米：
服务器的碰撞与子弹整体浮在地面上方 6 米。P2-1 验收之所以没暴露，
是因为两名玩家都浮在同一个高度、互射仍然成立（详见手册 P-17）。

做法：

- 新增 `Modules/Presentation/GroundProbe`：与玩家马达 `PlayerMotor` **同一套规则**
  （相同遮罩、相同"朝上才算地面"判据、相同 0.35 米抬升上限）的无状态地面采样；
- 玩家载体出生时用导航网格定高，之后每帧用 `GroundProbe` 贴地；
- 每次有人加入时留一条 Info 痕迹，让"两端高度是否对齐"在任何构建里都查得到。

验收证据（本机三进程，开发版构建）：

```
[服务器] 玩家 1 的碰撞载体就位：平面 (1.6, 0.0)，地面高度 -5.92 米。
[服务器] 玩家 1 已加入（在线 1 人）。
[服务器] 收到玩家 1 的首条输入：… 扳机=True …
[服务器] 玩家 1 开火（命中=True，目标=0）
客户端 B：[联机] 远端玩家 1 已进入视野。｜ 位置 (6.70, -1.28)
客户端 B：收到战斗事件：种类 2（受伤）… 种类 3（阵亡）
```

同时修掉上一批遗留的红灯：`SceneBootstrap.Multiplayer.cs` 达 419 行、超过工程 400 行上限，
按职责拆出 `SceneBootstrap.Multiplayer.Diagnostics.cs`（状态上报 + 自动行走）。
EditMode 全量测试 **492 / 492** 全绿。

### 20.4 服务器侧 AI 装配（2026-09-14）

AI 现在只在服务器上跑。具体做法与验收证据见
[Modules/04_AI.md 第 18 节](04_AI.md)（那是 AI 模块自己的交付记录，本章只记联机侧的影响）：

| 交付物 | 位置 | 说明 |
| --- | --- | --- |
| 共享敌人布局 | `Modules/AI/RaidEnemyLayout.cs` | 出生点 / 巡逻路线 / 生命护甲备弹，两端同一份 |
| 服务器 AI 装配 | `Bootstrap/Session/Network/ServerRuntime.Ai.cs` | 建 AI 世界、生成敌人、载体贴地、实体编号翻译 |
| 噪音接入 | `ServerRuntime.Ai.Noise.cs` | 枪声（事件）+ 脚步（0.25 秒采样），规则与客户端一致 |
| 验收辅助 | `ServerRuntime.Ai.Report.cs` | 首次交战记一笔；`-autowalk` 下每 5 秒汇总一次 |
| 多目标 | `Modules/AI/AiDirector.SetTargets` | 每名 AI 各取最近的玩家（单机的 `SetTarget` 保留） |
| 客户端 | `Bootstrap/SceneBootstrap.cs` | 联机客户端不再生成敌人（AI 属于服务器） |

验收证据（本机三进程，开发版构建）：

```
[服务器] AI 已就绪：5 名敌人，寻路=导航网格。
[服务器] AI 状态：…；敌人 0 在 (6.5, -4.7)，状态 Patrol；…       ← 服务器上真的在巡逻
[服务器] 敌人 0 进入交战：位置 (6.1, -4.4)，生命 100/100。        ← 枪声→调查→发现→交战
[服务器] 命中：敌人 0 → 玩家 1，伤害 9.0，剩余 0.0（已阵亡）      ← 权威致死
客户端 A：收到战斗事件：种类 1（来源 1000）                       ← 敌人编号 1000+
```

本阶段还统一了一处编号语义（详见排障手册 P-19）：**服务器侧所有战斗事件用"战斗单位编号"**，
由 `PlayerWeaponController.BindCombatant` 绑定；客户端保持玩家编号（表现层按本地玩家编号过滤枪声）。

### 20.5 敌人状态下行（2026-09-14）

| 交付物 | 位置 | 说明 |
| --- | --- | --- |
| 快照消息 | `Network/EnemyNetworkMessages.cs` | 每条含编号 / 位置 / 朝向 / 状态 / 生死 / 生命 |
| 通道 | `Network/EnemyNetworkChannel.cs` | 只下行；与移动通道分开（数据主人不同） |
| 服务器广播 | `Network/ServerRuntime.Ai.Network.cs` | 与玩家快照同节拍（20 Hz）发送 |
| 客户端接收 | `Bootstrap/SceneBootstrap.Multiplayer.Enemies.cs` | 位置进插值缓冲，状态与生死取最新值 |
| 远端表现 | `Presentation/AI/RemoteEnemyView.cs` | 复用敌人角色预制体、走路动画与状态颜色 |

几个刻意的取舍：

- **离散量不插值**：状态与生死取最新值。插值只会让"敌人已经进入交战"晚半拍，
  或者在阵亡瞬间画出一个半灰半彩的敌人；
- **地面高度由客户端自己采样**（`GroundProbe`）：联机客户端不再烘焙导航网格（那是 AI 的事），
  高度属于场景信息，不该由网络层携带；
- **阵亡的敌人继续出现在快照里**（`IsAlive=false`）：客户端据此保留尸体并停住它，
  否则敌人被打死后会从画面上直接消失，而玩家期待的是留下一具尸体。

验收证据（本机三进程，开发版构建；客户端 A 日志）：

```
[联机] 远端敌人 1000 已进入视野。（1001~1004 同样）
[联机] 远端敌人 1000 位置 (6.51, -4.73)，状态 Patrol，存活 True
[联机] 远端敌人 1001 位置 (18.74, 21.90)，状态 Patrol，存活 True
[联机] 远端敌人 1000 位置 (4.93, -0.69)，状态 Engage，存活 True    ← 位置在变、状态在切
[联机] 远端敌人 1000 位置 (5.60, 0.02)，状态 Patrol，存活 True
```

### 20.6 下一步：P2-3 表现对齐与总验收

- 弹道 / 枪口火焰 / 命中反馈 / 死亡动画两端一致（敌人已能开火，客户端目前只播枪声与射击动作）；
- 敌人被打死时的客户端表现需要一次完整走查（本批验收里没有敌人阵亡，这条分支尚未被走到）；
- 之后按设计文档 §12 走一遍完整「2 人对抗 AI」验收。
