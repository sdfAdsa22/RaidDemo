# M9 · P5 收尾与待办

> 更新时间：2026-09-15（P5 完成 + P-51 断线自愈 + P5.5 商店与任务权威化）
> 落地细节见 `Modules/10_联机.md` §27（P5）、**§28（P-51 断线自愈）**与 **§29（P5.5 商店与任务）**；
> 四条排障记录见 `排障记录/M9_问题记录与排障手册.md` 的 **P-50 / P-51 / P-52**。

## 一、P5 完成度

| 部分 | 状态 | 说明 |
| --- | --- | --- |
| P5-a 服务端进度存档 | ✅ | 房间共享仓库 + 按账号的金币 / 任务 / 随身装备，落 `-saveDir/profiles.json`；与单机存档隔离 |
| P5-b 容器权威化 | ✅ | `MultiplayerContainerLink` 供战局与安全屋共用；安全屋里的背包 / 弹药挂 / 仓库 / 装备槽全部走服务器 |
| P5-c 带入与结算 | ✅ | 进图按账号装备配发；撤离入共享仓库；阵亡清空；金币下发；新账号发基础装备；联机下商店暂时关闭（P5.5） |
| P5-d 掉线宽限与重连 | ✅ | 宽限（`-grace`，默认 60 秒）+ 重连接管；**P-51 断线自愈已落地**（服务器全员静默看门狗重建传输层 + 客户端自动重连），实机两场通过 |

**验证**：EditMode **641 / 641** 通过（P-51 新增 12 条规则测试）；P5 验收幕 1（进度 + 共享仓库 + 隔离）
与幕 2（重启后仍在）**全通过**；**P-51 断线自愈实机两场（安全屋 / 战局）三项判据全 PASS**；
P4.5-b 回归 **11 / 11** 全通过。

**P5.5（2026-09-15，同日后续）**：商店与任务改为服务端权威——购买 / 出售 / 批量出售 /
接取 / 追踪 / 上交 / 领奖七条意图上行，服务器用与单机同一批 handler 执行，
金币、容器、任务三样各走权威通道回发。**实机验收六项判据全 PASS**
（金币 5000 → 4940、共享仓库买后卖回空、任务进入进行中、0 条拒绝）；
EditMode **648 / 648**（新增 7 条协议测试）。验收辅助 `-autotrade`。
落地细节见 `Modules/10_联机.md` §29。

## 二、下一个要解决的事（按优先级）

1. **P6：云主机部署收尾** + 浏览器状态页 + 一键启动脚本（服务器在阿里云上已就绪，
   见 `10_联机.md` 相关章节）——**下一步开工项**。
2. **M10：热更新与分发**（自研启动器 + Addressables 资源热更 + HybridCLR 代码热更；
   启动器可切换连本地 / 云服务器下载更新，服务端带可视化界面）——P6 之后的大阶段。
3. **遗留可选项**（不挡任何验收，属深挖材料）：
   抓包验证 ICMP 假设（Wireshark 看 ICMP 与随后的 UDP 全丢）、关掉 Windows 的 UDP connreset 行为复测，
   以及"验收口径拆分"的书面化。自愈已在功能层覆盖该问题（见排障手册 P-51 的"解决"段）。

## 三、环境与脚本（下次直接可用）

- **批处理模式**（GUI 编辑器关闭时可用；GUI 打开时请用 `unity command --project-path ...`）：
  - 编译：`unity build 'D:\unity game\demo' --target StandaloneWindows64 -o 'D:\unity game\demo\Builds\StandaloneWindows64\RaidDemo.exe'`
  - 测试：`unity test 'D:\unity game\demo' --mode EditMode --output '...\p5_editmode_results.xml'`
  - 编辑器方法：`unity build 'D:\unity game\demo' --target StandaloneWindows64 --execute-method <类型.方法>`
    （例如重建中文字体：`RaidDemo.Bootstrap.Editor.UiAssetTool.BuildFontAsset`）
- **验收脚本**（`Library/ServerVerify/`，不入库）：
  - `p5_progress_check.ps1`（幕 1 / 幕 2 通过；幕 3 见 P-51）
  - `p45b_safehouse_check.ps1`（P4.5-b 回归，全绿）
  - `p5_diag_disconnect.ps1`（断线级联的诊断脚本：`-Scene safehouse|raid [-Reconnect] [-GraceSeconds N]`）
  - `p51_selfheal_check.ps1`（**断线自愈验收**：`-Scene safehouse|raid`，强杀乙 → 观察自愈 → 重启乙；
    两场三项判据全 PASS，见 §28.4）
  - `p55_merchant_check.ps1`（**商店权威化验收**：客户端 `-autotrade` 自动买 / 卖 / 接任务，
    脚本读服务器存档核对金币、共享仓库与任务状态；六项判据全 PASS，见 §29.4）
- **测试存档**：`Builds\StandaloneWindows64\server_saves_p5test`（验收用）、`server_saves_diag`（诊断用）；
  默认目录 `server_saves` 里还留着早前误用 `-saveDirectory` 跑出的一份数据，可清理。
- **常用启动参数**：服务器 `-server -map GreyboxRaid -port 7799 -saveDir <目录> -grace <秒> -autostart <秒> -spawnzone`；
  客户端 `-connect 127.0.0.1 -port 7799 -nickname <昵称> -passphrase 246810 -autoroom [-autowalk]`。
  **注意**：`-saveDir`（不是 `-saveDirectory`）、`-spawnzone` 是**服务器侧**参数；
  `-watchdog <秒>`（0 = 关闭自愈，1~30 可调，默认 2.5）也是**服务器侧**参数（P-51）；
  `-autotrade` 是**客户端侧**参数（P5.5 的验收辅助，需与 `-autoroom` 一起用）。

## 四、边界与刻意不做（避免下次误判为遗漏）

- 商店 / 任务界面的服务端权威化 = **P5.5**（联机下商店关闭并提示，单机不受影响）；
- 远端玩家外观仍复用本机角色（大厅外观同步未做）；图鉴仍是客户端本地记录；
- 联机进度与单机存档是两套账：联机期间客户端**不写**单机存档（已用哈希比对验证）。
