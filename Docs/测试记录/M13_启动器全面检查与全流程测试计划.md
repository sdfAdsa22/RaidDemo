# M13 · 启动器全面检查与全流程测试（只记录阶段）

> 状态：**只记录，未改任何产品代码、未重出产物**。
> 执行时间：2026-09-18 08:00 ~ 08:30（本机；08:00 前后电脑重启过一次，更新源已重新拉起）。
> 关联：`Docs/12_用户反馈问题记录.md` U-102（索引条目）；更新链路代码审计全文见
> `Builds/LauncherTestM13/m13-audit/M13_更新流程审计.md`（不入库）。
> GUI 链路本轮**没有**产出独立审计文件（该路代理未回传）；第三章的 GUI 条目来自本轮实机取证 +
> 我按需复核的代码行（每条的"根因/建议"都给了文件:行号）。

## 一、范围与基线

| 项 | 值 |
| --- | --- |
| 被测启动器 | `Builds/交付/RaidDemo-1.0.6/启动器/RaidDemo.Launcher.exe`（自包含单文件） |
| 基线哈希 | SHA256 `FDA712EA409B8760EA9A295D653A1162146C30B91843237ECFF1B038B23953D8`（冻结后未再变） |
| 被测更新源 | 本机 `http://127.0.0.1:8090`（= `Builds/Update/1.0.6`）；云 `http://47.104.210.207:8090` |
| 内容版本 | 本体 1.0.6 / 资源 1.0.6.c2d0c5e56（两个源一致） |
| 全量口径 | 更新计划 242 个文件 / 158.4 MB；安装完成后落盘 244 个文件（含 `.raiddemo` 快照与备份目录） |
| 测试沙盒 | `Builds/LauncherTestM13/**`（脚本与产物均在该目录，不入库）；脚本在 `Library/TestRuns/M13/`（不入库） |
| 未触碰 | 用户现有安装、存档、云主机（只读访问 8090 清单/内容） |

## 二、结论速览

- 启动器主链路（检查更新 → 全量/增量下载 → 校验 → 落盘 → 开始游戏）**全部跑通**：本机源与云源各一轮；
  中文+空格路径、相对路径、换源、失败回退、备份轮换、staging 清理、游戏运行中拒绝更新均符合预期。
- 本轮新发现 **2 个 P1 代码缺陷**（并发互斥缺失、清单路径越界）、**1 个 P1 GUI 缺陷**（手输安装目录后直接保存会静默丢失设置），
  外加 P2 1 条、P3 8 条（详见第三章）。
- 最需要优先修的是 M13-02（路径越界，安全级）与 M13-01（并发互斥）。

## 三、问题清单

### M13-01（P1）同一安装根并发更新无互斥；启动器无单实例保护

- **现象**：两个启动器同时更新同一安装根 → 必有一方失败，报错为原始英文；日志出现"校验大小不符"，即读到了另一进程写入的字节。
- **证据**：
  - `Builds/LauncherTestM13/n7b-concurrent/`：JOB1 退出码 1，`替换文件失败（已回滚）：Could not find file '…\staging\body\cross_saves_A旧服务器新客户端\accounts.json'`；JOB2 退出码 0（本轮我实测）。
  - `Library/TestRuns/M13/followups.txt`：并发两次 `--update` → 进程1 退出码 0、进程2 `The process cannot access the file 'accounts.json' because it is being used by another process.`。
  - GUI 侧（本轮实测）：双击两次 exe → 两个独立进程、两个"RaidDemo 启动器"窗口并存，各自都能点"下载"。
- **根因**：暂存路径只由"安装根+层+相对路径"决定（`Tools/Launcher/Update/UpdateApplier.cs:77`），每个进程开头清空整层暂存
  （`UpdateApplier.cs:166` / `UpdateSession.cs:249`），而校验在下载中、落盘在全部下载完成后（`UpdateSession.cs:333`），
  中间窗口可达数分钟；全工程无跨进程互斥（`rg Mutex Tools/Launcher` 无命中）。
- **建议**：命名 `Mutex`（键含安装根）+ staging 按会话隔离 + 落盘前复校；GUI 另加单实例（激活已有窗口）。

### M13-02（P1）清单路径未做归位校验，可写出/删除安装根之外

- **现象**：清单里放 `path = "..\escape-probe.txt"` 的条目，`--update` 会**在安装根上一级写文件**并返回退出码 0；
  把该条目移除再跑，该外部文件会被**删除**。
- **证据**：`Builds/LauncherTestM13/m13-audit/traverse-src/`（含 `escape-probe.txt` 与清单）、`traverse-root/`；
  审计报告 `M13_更新流程审计.md` F2 记录了完整命令与结果。
- **根因**：`ManifestFileEntry.NormalizePath`（`Assets/Game/Kernel/Pure/Updates/UpdateManifest.cs:166`）不处理 `..`；
  目标路径直接 `Path.Combine(installRoot, change.Path)`（`UpdateSession.cs:333`），删除同理（`UpdateApplier.cs:271`）；
  盘符绝对路径会按 `Path.Combine` 语义替换安装根。
- **建议**：统一 `ResolveInsideRoot`；清单解析期拒绝 `..`/盘符/绝对路径条目；补 EditMode 负例。

### M13-03（P1）设置抽屉：手输"安装目录"后直接点"保存设置"会静默丢设置

- **现象**：在"安装目录"输入框里键盘输入新路径 → 直接点"保存设置" → 界面提示"设置已保存"、日志写"已保存设置"，
  但 `launcher.config.local.json` 里 `InstallRoot` **仍是旧值**、新目录未创建；输入框仍显示新路径 → 界面与实际不一致。
- **对比实验**：同样输入后先按 `Tab`（让输入框失焦）再保存 → 配置正确写入，且自动追加 `RaidDemo` 子目录、目录被创建。
- **根因（初判）**：`InstallRootRow.m_Text.Leave += Apply(...)`（`Tools/Launcher/InstallRootRow.cs:79`）；
  "保存设置"是自绘 `Panel`（不抢焦点），点它不会触发 `Leave`，`TrySaveSettings` 直接落盘旧值
  （`LauncherForm.Drawer.cs` 的 `TrySaveSettings`）。
- **建议**：保存前显式提交编辑框（调用与 `Apply` 相同的路径），或把 Apply 挂到 `TextChanged`/`Validated`。

### M13-04（P2）内容缓存与 `state.json` 是机器级共享，后写覆盖先写

- **现象**：不同安装根共用 `%USERPROFILE%\AppData\LocalLow\RaidDemo\RaidDemo\content`；`state.json` 直接覆写，无协调。
- **证据**：`ContentCacheInstaller.ResolveGameDataRoot`（`Tools/Launcher/Update/ContentCacheInstaller.cs:165`）只用公司名/产品名；
  `WriteState`（同文件 142 行）`File.WriteAllText`；本机 `state.json` 的 `updatedAt=2026-09-18T08:07:35` 由 `gui-run1` 写入。
- **建议**：临时文件 + 原子替换；并入 M13-01 的互斥区；多安装根场景考虑按安装根隔离（需与游戏侧同步）。

### M13-05（P3）失败原因把 .NET 英文原文直接透给玩家

- **证据**：`N5 安装根是文件`：`更新失败：Cannot create '…' because a file or directory with the same name already exists.`；
  `N7b`：`Could not find file '…'`；并发用例：`The process cannot access the file … because it is being used by another process.`
  （GUI 弹窗、CLI 输出、日志三处同源）。
- **建议**：`IOException/UnauthorizedAccessException` 等分类翻译为中文并附"下一步怎么办"。

### M13-06（P3）清单缺 `body` 层被当成"已是最新版本"

- **证据**：`N4`（清单只有 `schemaVersion/generatedAt`）：`退出码=0 | 已是最新版本（本体 未知）。`；
  `N11`（空 files + version 9.9.9）：`退出码=0 | 已是最新版本（本体 9.9.9）。`
- **影响**：源发布错误看起来像"成功且最新"。
- **建议**：缺 `body`、`files` 为空、`version` 缺失时判为清单无效并报错退出。

### M13-07（P3）失败/中断后暂存区残留

- **证据（本轮实测）**：云源全量下载中途关窗 → 安装根只有 `.raiddemo/staging/` 下 31 个半成品文件（32 文件/34 MB），**无本地清单快照**；
  审计侧失败用例残留 153 MB 级；只有下次更新开头才清理。重开后再点"下载"能正常收敛（244 文件、快照 1.0.6、staging 目录消失）。
- **建议**：进程退出/启动时清理本会话 staging；失败路径主动清理（保留"续传"能力的话按会话目录管理）。

### M13-08（P3）隐藏源的两处口径不一致

- **证据**：选中"云主机"（`hideAddress=true`）时，"游戏服务器"显示`（已隐藏）`，而"更新源地址"输入框是**空白**（截图见本轮记录）。
- **建议**：地址框同样显示`（已隐藏）`占位符。

### M13-09（P3）长状态文案被右边缘裁切

- **证据**：`发现更新：242 个文件 / 158.4 MB（远端 本体 1.0.6 / 资源 1.0.6.c2d0c5e56）。` 与
  `更新完成：242 个文件（本体 1.0.6 / 资源 1.0.6.c2d0c5e56）。` 在 100% 缩放下都被截断（`…/ 资源 1.0.6.c2d0c5e56）` 之后不可见）。
- **建议**：缩短口径（例：`本体 1.0.6 · 资源 c2d0c5e56`）或允许换行/两行。

### M13-10（P3）忙碌态按钮视觉无变化

- **证据**：下载中三个按钮在 UIA 里均为 `(disabled)`、功能确实禁用，但截图里"开始游戏"仍是高亮青色，看不出被禁用。
- **建议**：禁用态加灰化/降透明度。

### M13-11（P3）日志窗口打开后主界面图标压在抽屉之上

- **证据**：打开日志窗口后再展开设置抽屉，主界面右上角图标（关闭/最小化/设置/日志）重新绘制在抽屉"收起"按钮上方（截图可见齿轮图标覆盖收起按钮）。
- **建议**：抽屉展开时对主 chrome 做 `SendToBack` 或统一 z 序/重绘。

### M13-12（P3）CLI 输出编码依赖宿主，非 UTF-8 捕获时中文乱码

- **证据**：`Start-Job` 捕获子进程输出时出现 `鏇存柊婧愶細…`（UTF-8 字节按 GBK 解码）；直接在当前控制台运行正常。
  `Program.cs:52-61` 用 `UTF8Encoding(false)` 包装 `Console.Out`（无 BOM）。
- **建议**：脚本侧约定 `[Console]::OutputEncoding = UTF8`（本轮脚本已如此）；若要兼容任意宿主可考虑 BOM 或 ASCII 兜底。

### M13-13（P3，待复现确认）同秒内连续多次更新时备份目录数偏少

- **证据**：`Library/TestRuns/M13/followups.txt` 实验 1a（无间隔连续三次更新）`备份目录数=0`；
  实验 1b（间隔 1.3 s）`=1`；`N13`（脚本内三次增量）`=2`（符合"保留两份"设计）。
- **疑点**：备份目录以秒级时间戳命名，同一秒内的多次更新可能互相覆盖/复用。
- **建议**：命名加序号或毫秒；修复前先按脚本复现一次确认。

### M13-14（P3，观察）相对 `--root` 的解析基准是"当前工作目录"

- **证据**：`N9`：`--root relative-root-m13` 的落点在 cwd（True），不在启动器目录（False）。
- **说明**：CLI 语义如此（便于脚本），但文档未写；GUI 默认 `InstallRoot="RaidDemo"` 解析到 **exe 所在目录**。
- **建议**：在 `--help`/文档里写明两者的基准差异。

## 四、测试矩阵与执行记录

### A · 静态审计（两路并行，只读）

| 路 | 范围 | 结果 |
| --- | --- | --- |
| 更新链路 | `Update/*.cs`、`LauncherConfig.cs`、`Program.cs`、`LauncherLog.cs` | 完成；报告 `Builds/LauncherTestM13/m13-audit/M13_更新流程审计.md`；产出 M13-01/02/04/05/06/07 |
| GUI 链路 | `LauncherForm.*`、`InstallRootRow.cs`、`LogWindowForm.cs`、`Theme/*` | 未产出独立文件；GUI 结论以第三章实机取证为准（涉及代码行的条目已就地复核） |

### B · CLI 自动化

- 复用 `Tools/Launcher/verify/p2_launcher_check.ps1`：**13/13 PASS**（A 全量 / B 增量 / C 换源 / D 失败回退），记录见 `Library/TestRuns/M13/p2_launcher_check.txt`。
- 新增负例组（`Library/TestRuns/M13/negatives.ps1`，14 条）：**14/14 PASS**（N1 不可达、N2 404、N3 清单损坏、N4 缺 body、N5 根是文件、
  N6 中文+空格路径全量、N2.1 源不可达但安装完好、N11 空清单、N12 资源层缺文件、N8 `--force`、N9 相对根、N7 并发、N13 备份轮换、N14 staging 清理），
  记录 `negatives2.txt`、`n7b.txt`。
- 结论：CLI 侧失败回退不变量成立（失败时安装文件与快照不变、退出码 1、文案可读）；并发与清单路径两类问题见 M13-01/02。

### C · GUI 实机（computer-use，截图取证；本机当前 DPI 档）

| 用例 | 结果 |
| --- | --- |
| 三按钮（检查更新/下载/开始游戏） | PASS：`发现更新：242 个文件 / 158.4 MB` → 进度 `x/242 · y/158.4 MB` → `更新完成` → 开始游戏拉起 `RaidDemo.exe` |
| 忙碌态禁用 | PASS（功能），视觉提示缺失见 M13-10 |
| 设置抽屉：切源 | PASS：本机 ↔ 云主机 ↔ 自定义；地址框与"游戏服务器"联动；非自定义源地址框拒绝输入（只读生效） |
| 隐藏源 | PASS（`（已隐藏）`）；地址框口径不一致见 M13-08 |
| 浏览…选目录 | PASS：自动追加 `RaidDemo` 子目录、即时创建并落盘；手输+直接保存见 M13-03 |
| 保存设置 / 恢复默认 | 保存 PASS（Tab 提交路径）；恢复默认按钮存在（未逐字段比对） |
| 日志窗口 | PASS：内容含检查/更新/快照/资源层各行；`在资源管理器中打开`；路径 `…\.raiddemo\launcher.log`；Esc 关闭 |
| 无边框拖动 / 最小化 / 还原 | PASS：拖动位移与拖拽量一致（origin 780,410 → 660,320）；最小化后 `get_window_state` 报 `window is minimized`，还原后位置不变 |
| 关闭 / 二次启动 | 关闭 PASS（进程退出）；二次启动 → 两个窗口并存（M13-01） |
| 下载中关窗 | PASS（进程立即退出）+ 残留见 M13-07；重开后续传收敛 |
| 首次启动抽屉位置 | 观察：某次冷启动的首次捕获里抽屉面板出现在窗口**左侧**（x≈0，含全部设置控件），怀疑构造后未立即定位到右侧闭合位；后续冷启动未复现 |

### D · 端到端

- 本机源：空根 → 全量（242 文件）→ 复检"已是最新" → 开始游戏 → 主菜单 → 新游戏 → **安全屋**（截图已存）。
- 云源：切到"云主机"（隐藏地址）→ 复检"已是最新" → 空根全量下载（242 文件/158.4 MB）→ 更新完成。
- 游戏侧启动参数：由启动器按所选源注入 `-serverhost`（云主机档位 = 47.104.210.207），本轮未在游戏内跑联机进图；
  联机进图/撤离结算引用 M11/M12 报告，未在 M13 重复。

### E · 恶劣环境

| 用例 | 结果 |
| --- | --- |
| 源不可达 / 404 / 清单损坏 | PASS：中文报错、退出码 1、安装完好（N1/N2/N3/N2.1） |
| 资源层文件缺失 | PASS：退出码 1，本体文件哈希不变（N12） |
| 中文+空格路径全量安装 | PASS（`Builds/LauncherTestM13/中文 目录/游戏`，用时 1 s，快照 1.0.6） |
| 备份轮换（连续三次变更） | PASS：保留 2 份（N13）；同秒连续更新的边界见 M13-13 |
| staging 清理 | PASS：成功更新后 staging 文件数 0（N14） |
| 游戏运行中拒绝更新 | PASS：CLI 退出码 1 + `检测到 RaidDemo 正在运行，文件会被占用。请先退出游戏，或加 --force 跳过检查。`；安装未动 |
| 下载中断（关窗）→ 重开续跑 | PASS：见 M13-07 证据与收敛结果 |
| 云源短暂不可达 | 未单独构造（云源全程可达；错误文案由 N1/N2 覆盖） |
| 150% DPI 全流程 | **未执行**（需改系统缩放；留到修复阶段回归） |

## 五、未覆盖 / 待办

1. 「更新源不可用导致检查失败后，旧版本仍可启动」——CLI 侧已取证（N2.1），**GUI 路径待复测**。
2. 游戏内自更新（跨内容版本）未修（M12 遗留），本轮仍按"启动器绕过"口径记录。
3. 150% DPI 与 4K 缩放回归。
4. M13-13（同秒备份）需按脚本复现确认。
5. 首次启动抽屉位置（M13-11 之外的 z 序/定位观察）需二次复现。

## 六、复现环境与排障

- 本机更新源：`Tools/UpdateSource/RaidDemo.UpdateSource.exe`（监听 127.0.0.1:8090，根目录 `Builds/Update/1.0.6`）。
  电脑重启后需重新拉起；判断是否在线：`Invoke-WebRequest http://127.0.0.1:8090/manifest.json` 应 200。
- 云源：`http://47.104.210.207:8090`（管理页 `http://47.104.210.207:8080/`，口令见 `Docs/12_用户反馈问题记录.md` U-101）。
- 沙盒与脚本：`Library/TestRuns/M13/*.ps1|txt`（不入库）；审计材料 `Builds/LauncherTestM13/m13-audit/`（不入库）。
- 已知环境噪声：`Start-Job` 捕获子进程输出会 GBK 误码（M13-12），脚本里需要 `[Console]::OutputEncoding = UTF8`。
- 启动器 GUI 无 UIA 元素名时，可用窗口相对坐标操作；日志窗口用 `Esc` 关闭；抽屉展开时右上角图标不可见，关闭窗口需先"收起"。

## 七、后续"完美无误"建议顺序（待确认后执行）

1. 修 M13-02（路径越界，安全）→ 2. 修 M13-01（互斥 + 单实例）→ 3. 修 M13-03（保存丢设置）→ 4. 其余 P2/P3。
2. 回归判据：EditMode 全量绿 + `p2_launcher_check.ps1` 13/13 + 负例组（含新增并发/越界/保存三条自动判据）+ C/D 两组实机复测。
3. 通过后重出 1.0.7 三件套、重发双更新源、云主机重部署并复验。
