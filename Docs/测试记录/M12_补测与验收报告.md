# M12 · 补测与验收报告（1.0.1 → 1.0.2）

> 状态：**执行中**（2026-09-16 开测）
> 计划见 [M12_补测与验收计划.md](M12_补测与验收计划.md)；
> 工具链问题见 `Docs/排障记录/M12_问题记录与排障手册.md`。

## 一、基线与产物

| 项 | 值 |
| --- | --- |
| 代码基线 | `8410cf2`（M11 云冒烟记录） |
| 起始版本 | `1.0.1` |
| 目标版本 | `1.0.2` |
| 被测交付（回归基线） | `Builds/交付/RaidDemo-1.0.1` |
| 云主机 | `47.104.210.207`（7777 游戏 / 8080 状态页 / 8090 更新源） |
| 测试存档 | `server_saves_e2e`（新增账号 `m12a~m12d`，保留 `cloud01~04`） |
| 证据目录 | `Library/TestRuns/20260916-2259` |

**1.0.1 交付三件套哈希（开测时复核）**：

| 产物 | SHA-256 |
| --- | --- |
| 客户端 `RaidDemo.exe` | `A4C71B1BFC3E42D02F2F129FE04ADEF3EBF2343A662884E4B99598E9DFC90E7D` |
| 服务端 `RaidDemoServer.exe` | `5C49CF961542B72191886BAC1D88AA6AC5DC85C0F76D647E3A78ADB522D0FEB1` |
| 服务端面板 `RaidDemo.ServerHost.exe` | `7376EF4FE1F513B02D25E7AEE679A66FC5F32E94210B3463B500F4800EDDF99C` |
| 启动器 `RaidDemo.Launcher.exe` | `145AB351069B97B4BCAFDEF7EAA71704FEEB7441D039C010F51FB230113DE55A` |

## 二、批次 0 · 基线冻结与环境体检

| # | 步骤 | 结果 | 证据 |
| --- | --- | --- | --- |
| 0.1 | 冻结基线（HEAD / 版本 / 脏文件） | PASS | `8410cf2`、`1.0.1`；脏项 6 个（见下） |
| 0.2 | 归档 `Docs/计划.md` | PASS | 与正式版 `M11_三版本全流程实机测试计划.md` 逐行比对后确认是重复草稿，原件已入回收站 |
| 0.3 | 归档 `Docs/问题.txt` | PASS | 原文已并入 `Docs/12_用户反馈问题记录.md` §36 附录，原件已入回收站 |
| 0.4 | 忽略 Linux 构建产物 | PASS | `.gitignore` 新增 `Assets/AddressableAssetsData/Linux*` |
| 0.5 | 复核云主机 | PASS | 状态页：`Listening=True`、`Port=7777`、`SaveDirectory=server_saves_e2e`、在线 0 人 |
| 0.6 | 复核云更新源 | PASS | `/api/verify?version=1.0.1` → `Passed=true, CheckedFiles=242, Problems=[]` |
| 0.7 | 建证据目录 | PASS | `Library/TestRuns/20260916-2259` |

**脏项处置**：`Packages/manifest.json`、`Packages/packages-lock.json`、
`ProjectSettings/UnityConnectSettings.asset` 三项是 Linux 服务端构建引入的产物，
且本轮还要再出一次 Linux 服务端，因此按"最后统一还原"处理，放在批次 5 收尾一次做完。

## 三、批次 1 · Unity 编辑器补测（单机域）

### 3.1 回归

| 套件 | 结果 | 说明 |
| --- | --- | --- |
| EditMode | **716 / 716 通过** | 与 M11 基线一致，改动整理按钮后仍全绿 |
| PlayMode | **8 / 8 通过** | 原 4 条 + 本轮新增 4 条 |

### 3.2 新增 PlayMode 用例（补 M11 的 BLOCKED 与未覆盖项）

新增 `Assets/Game/Tests/PlayMode/InventoryUiInteractionPlayModeTests.cs`，
用输入系统的测试夹具（`InputTestFixture`）直接产生鼠标设备事件，
驱动真实界面（界面读的就是 `Mouse.current`，不是替身）：

| 用例 | 覆盖的未覆盖项 | 结果 |
| --- | --- | --- |
| 拖拽把仓库里的物品搬进背包 | 背包拖拽（M11 判 BLOCKED） | PASS |
| 双击把仓库里的物品快速转移进背包 | 双击快速转移（M11 判 BLOCKED） | PASS |
| 点整理仓库按钮后物品按面积重排到左上角 | 本轮新功能「整理仓库」 | PASS |
| 战利品箱界面不显示整理仓库按钮 | 新功能的显隐规则 | PASS |

> 说明：M11 的 BLOCKED 是因为系统级鼠标注入无法稳定复现拖拽；改用设备事件后
> 两条路径都拿到达成了可重复的自动化证据，后续不再需要靠人工点。

## 四、批次 2 · 联机多进程补测 + 修复

### 4.1 修复与新功能

| 项 | 内容 | 验证 |
| --- | --- | --- |
| 仓库整理（`问题.txt` #5） | 仓库界面标题栏 + 商人界面仓库下方各加常驻「整理仓库」按钮，复用 `InventorySortIntent`；仅右栏是仓库时出现 | PlayMode 用例 2 条（点击后按面积重排、战利品箱界面不显示）+ 云实机截图可见按钮 |

### 4.2 联机项的验证口径

本轮**没有**再跑一遍"编辑器多进程现场"，改用"已有自动化覆盖 + 云上实机"两条线交叉：

| 未覆盖项 | 本轮结论 | 依据 |
| --- | --- | --- |
| 倒地 / 救援规则 | 规则层已覆盖 | `PlayerLifeStateTests`（6 条：失能、读条、超时死亡） |
| 房间管理（口令错 / 踢人 / 主动退房） | 规则层已覆盖 | `LobbyRoomTests`（16 条）+ `LobbyLimitsTests`（10 条） |
| 多人抢同一容器 | 同步层已覆盖 | `LootContainerSyncTests`（12 条：容量差异、同容器坐标、实例复用） |
| 共享安全屋协议 / 场景约定 | 已覆盖 | `P45SharedSafeHouseTests`（4 条） |
| 救援成功的端到端（E 键读条救起队友） | **未覆盖** | 需要两名真人/注入配合，本轮未跑 |
| 门禁负例（队友在战局中时房主禁止开图） | **未覆盖** | 需要构造"一人已在图、一人在屋"的现场 |

> 结论：规则层不缺覆盖，缺的是"多人现场的手感与端到端"。这两条按计划继续挂在未覆盖清单里。

## 五、批次 3 · 1.0.2 产物与本地冒烟

### 5.1 产物

| 产物 | 结果 |
| --- | --- |
| 客户端 `Builds/Update/1.0.2` | body 242 文件 / 158.4 MB |
| 更新清单 | `body 1.0.2`（242 文件）+ `content 1.0.2.c540b047e`（10 文件） |
| Windows 专用服务器 | `Builds/ServerWindows`（托管程序集 09-17 06:13 刷新，147.2 MB / 50 秒） |
| Linux 专用服务器 | `Builds/ServerLinux`（09-17 06:15 刷新，149.2 MB / 69 秒） |
| 交付目录 | `Builds/交付/RaidDemo-1.0.2`（客户端 / 服务端 / 启动器 + 说明；已回补 `launcher.config.local.json`） |

### 5.2 本地打包冒烟（1 服务端 + 2 个打包客户端）

证据：`Library/TestRuns/20260916-2259/smoke-server.log`（客户端日志同名 a/b）。

| 检查点 | 结果 | 证据 |
| --- | --- | --- |
| 服务端版本 | PASS | `本机版本标识：1.0.2` |
| 登录（AR-07 修复面） | PASS | `口令校验 653 ms / 757 ms（后台线程）` |
| 主线程卡顿 | PASS | 全日志**零** `主线程卡顿` 记录 |
| 建房 / 双人进图 | PASS | 状态页 `room=默认房间 host=m12a online=2 world=战局 InWorld=2`；日志 `战局开始：2 名玩家进入房间「默认房间」` |

> 本轮构建期间遇到 Unity 编译僵死，处置过程见 `Docs/排障记录/M12_问题记录与排障手册.md` T-01。

## 六、批次 4 · 云上全流程实机

环境：云主机 `47.104.210.207`（2 核 / 2 GiB），更新源 8090、状态页 8080、游戏 7777；
存档沿用 `server_saves_e2e`（共享仓库 2 件历史物品保留，验证"旧档 + 新账号"组合）。

### 6.1 部署

| 步骤 | 结果 |
| --- | --- |
| 上传 Linux 1.0.2 服务端（`scp` 覆盖） | PASS |
| 覆盖最新 `deploy/server.sh` 并 `chmod +x` | PASS（T-10 教训落实） |
| chroot 启动（`RAIDDEMO_CHROOT=/root/ubuntu-2204`） | PASS：`本机版本标识：1.0.2` |
| 旧存档保留 | PASS：`房间共享仓库已就绪：2 件物品` |

### 6.2 两个实机客户端（窗口化）连云

用交付版客户端 `-connect 47.104.210.207 -port 7777 -nickname m12a/m12b -passphrase 123456 -autoroom`，
服务端临时把 `autoStart` 设为 90 秒以便无人值守地进入战局（测完已改回 0）。

| 检查点 | 结果 | 证据（`Library/TestRuns/20260916-2259/small/`） |
| --- | --- | --- |
| 双人进共享安全屋 | PASS | `cloud-safehouse-a.jpg`（角标「2/4 人 · 房主 · 全员在屋」） |
| 1.0.2 新功能「整理仓库」在真机可见 | PASS | `cloud-stash-a.jpg`（标题栏右侧出现按钮；仓库显示历史 AK + 弹药） |
| 自动开局进图 | PASS | `cloud-raid-a.jpg` / `cloud-raid-reload-a.jpg`（HUD：生命 100/100、护甲满、`AK-74 步枪 · 5.45 30/30`、负重 4.3/50kg） |
| 战局内换弹后 HUD | PASS | `cloud-raid-reload-a.jpg`（按 R 后弹药回到 30/30） |
| 暂停菜单 | PASS | `cloud-pause-b.jpg`（返回主界面（放弃本局）/ 返回桌面） |
| **AR-06 复验**：暂停 → 返回主界面 | PASS | `cloud-backmenu-b.jpg`（回到「联机」界面，不再是卡在安全屋） |
| 联机界面 → 主菜单 | PASS | `cloud-mainmenu-b.jpg`（RAID DEMO 主菜单） |

> **口径说明**：1.0.2 里"返回主界面"的落点是**联机界面**（可再点「返回主菜单」或按 Esc 回主菜单），
> 与 AR-06 期间"卡在安全屋、Esc 弹游戏暂停"的故障状态完全不同，判定为修复成立。

### 6.3 本轮未覆盖（继续挂账）

- 旧客户端版本握手负例：M11 已在 1.0.0 上 PASS，且该代码路径本轮未改动，未复测。
- 启动器「选云主机 → 全量下载 → 安装」完整 UI 路径：未复测（更新源 `verify` 已确认 242/0）。
- 云上撤离 / 阵亡结算闭环：本轮止步于"进图 + 战斗 HUD"，未跑到撤离点。

### 6.4 云上 4 人完整局（2026-09-17 补跑，含 AR-09 修复验证）

首次 4 人局在开图瞬间全体静默，根因见第九节（AR-09）。修复"交付客户端随包带资源层"后重跑：

| 检查点 | 结果 | 证据（云主机 `Logs/server.log`） |
| --- | --- | --- |
| 4 人进共享安全屋 → 自动开局进图 | PASS | 状态页 `online=4 world=战局 inWorld=4` |
| 撤离结算 + 入库（玩家 3） | PASS | `结算：玩家 3 撤离成功，带出价值 18450`、`撤离入库：3 件物品，共享仓库现有 5 件` |
| 撤离结算 + 入库（玩家 4，无头机器人自己走进撤离区） | PASS | `结算：玩家 4 撤离成功，带出价值 10410`、`撤离入库：3 件物品，共享仓库现有 8 件` |
| 超时结算口径（AR-05 复验） | PASS | `战局时长到达上限（300 秒）：2 名玩家按超时结算` + `结算：玩家 2 时间耗尽，损失价值 9450`（不再是"带出价值"） |
| 击杀归属（AR-05 复验） | PASS | 全程 `击杀 0`——本局 AI 互射没有被记到任何玩家头上 |
| 全员结算 → 回共享安全屋 | PASS | `本局结束：全员已结算，房间回到等待状态（等待中的成员 4 人）`、状态页 `world=共享安全屋 inWorld=4` |

> 仍未覆盖：**救援成功**（倒地→队友读条救起，需要两名真人配合）与**门禁负例**
> （队友在战局中时房主禁止开图）——本轮没有构造出这两条现场。

## 七、批次 5 · 发布与收尾

| 项 | 结果 |
| --- | --- |
| 云更新源 | `upload` 242 文件 → `publish 1.0.2` → `verify Passed=true / CheckedFiles=242 / Problems=[]` |
| 本机更新源 | 同上（`127.0.0.1:8090`，口令 `rd-demo-2026`） |
| 交付目录 | `Builds/交付/RaidDemo-1.0.2`（含启动器本地配置，云 / 本机两套源都在） |
| 云主机复位 | `autoStart` 改回 0 并重启，仍运行 1.0.2 |

## 八、遗留与延后

- ≥ 2 小时长时间稳定性与内存增长：本轮不测，登记延后。
- `0.10.1 → 1.0.0` 历史存档兼容：本轮不测，登记延后。

## 九、云上 4 人局中断的根因与修复（AR-09）

### 9.1 现象

2026-09-17 云上 4 人局（2 窗口 + 2 无头）在开图瞬间全体静默：服务端 P-51 看门狗连续重建
传输层 5 次，60 秒宽限到期后 4 人全部掉线、房间解散；客户端进程仍在，但不再上行。

### 9.2 根因（客户端日志，确定性复现）

客户端日志停在进图那一刻，紧接着是 Addressables 报错：

```
[联机] 加载战局地图：GreyboxRaid
RemoteProviderException : Invalid path in AssetBundleProvider:
  '.../客户端/RaidDemo_Data/StreamingAssets/aa/StandaloneWindows64/<bundle>.bundle'
[热更] 按地址取资产失败（data/itemcatalog）：...
```

原因是**交付客户端只有本体层、没有资源层**：构建时资源被拆成独立内容层
（`Builds/Update/<版本>/content/`，共 12 个文件 / 14.3 MB），交付目录里的客户端只有
`catalog.bin` 与两个内建包。于是：

- 更新源不可用、本地缓存又是旧版本时，进图会去 `StreamingAssets/aa/<平台>/` 找内容包 → 找不到 → 进图失败；
- 直接双击交付客户端能玩，只是因为开发机上**已有历史内容缓存**，新机器上不复现这个"幸运"。

补充实验（单客户端本地复现）：

| 场景 | 结果 |
| --- | --- |
| 交付客户端 + 更新源不可用 + 无本地缓存 | 进图失败（Invalid path） |
| 交付客户端 + 更新源可用（客户端自己下载新内容） | 进图失败；换到无空格路径则**原生崩溃** |
| 交付客户端 + 资源层已随包提供 + 更新源不可用 | **进图成功**（`inWorld=1`，上行心跳持续） |

### 9.3 已做修复

`Tools/Release/assemble_delivery.ps1` 现在会把资源层一并复制进交付客户端
（`客户端/RaidDemo_Data/StreamingAssets/aa/StandaloneWindows64/`），
交付目录因此"解压即玩、离线可用"，同时不影响更新源可用时的热更。

### 9.4 仍未解决（下一轮）

**客户端自己下载并应用新内容后，第一次进图仍会失败**（无空格路径下是原生崩溃）。
已确认不是路径空格问题，而是"新内容生效后首次场景加载"这条路径本身。
下一轮需要动代码：在应用下载内容后、进入下一个场景前，把新 catalog 的寻址状态
（或 Addressables 的 catalog 重载时机）与场景加载对齐，并补一条"热更后进图"的回归用例。
