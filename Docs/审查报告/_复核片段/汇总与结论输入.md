# 第七节结论与汇总表的输入（由 verify_p1_core 汇总）

> 用途：主文档 [`RaidDemo_审查问题状态复核.md`](../RaidDemo_审查问题状态复核.md) 第二节汇总表与第七节结论可直接采用本片段。
> 口径：只统计 72 条编号问题；`部分修复 / 口径变化` 统一计入**口径变化**（行为已变、原结论不再准确，但也不算改对），
> 并在备注里点名是哪几条，避免把"部分修复"当成"已修"结账。

## 一、第二节汇总表（可直接替换）

| 领域 | 条数 | 仍存在 | 已修 | 口径变化 | 无法判定 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Session / Lobby / Network（`041`~`057`） | 17 | 13 | 2 | 2 | 0 |
| Kernel / Data / Simulation / Inventory / Combat / Save（`010`~`026`） | 17 | 15 | 0 | 2 | 0 |
| AI / Raid / Meta / Presentation / UI / Bootstrap / Editor（`027`~`040`、`058`~`063`） | 20 | 19 | 0 | 1 | 0 |
| 架构 / 规范 / 依赖 / 文档（`001`~`008`、`064`~`073`） | 18 | 17 | 0 | 0 | 1 |
| **合计** | **72** | **64** | **2** | **5** | **1** |

备注（写进表下脚注）：

- **已修 2 条**：`RD-AUD-050`（全员结算后回房间已接线）、`RD-AUD-052`（同 tick 重复结算已有守卫）。
- **口径变化 5 条**：`043` / `046`（部分修复：守卫与 try/catch 兜底改变了后果，但核心行为仍不成立）、
  `012`（服务器事件已带弹匣余量，残留覆盖度问题）、`024`（"未拒绝负散布"是原报告误判，其余子项成立）、
  `063`（`ReturnFromRaid` 已随 `050` 消解，剩死字段与注释漂移）。
- **无法判定 1 条**：`RD-AUD-006`（asmdef 多余引用需逐程序集反查，本轮未做）。
- 也就是说：**72 条里真正被后续提交修掉的只有 2 条**，其余 70 条要么仍成立，要么口径需要重写。

## 二、第七节结论（建议正文）

### 1. 总账

复核结论一句话：**原报告没有"过期作废"到不能用的程度**——72 条里 64 条在当前 HEAD 上依然成立，
5 条需要改写结论（其中 2 条是原报告误判或口径偏差），只有 2 条已被后续提交真正修掉。
所以修复工作应当**继续沿用原报告的编号清单**，不需要重新做一轮审查。

### 2. 按性价比分档

| 档位 | 内容 | 判断依据 |
| --- | --- | --- |
| **A. 先修（P1 数据完整性与联机权威）** | `041` `042` `043` `044` `045` `049`（联机权威链）；`027` `028` `029` `010` `011`（核心数值与物品状态）；`046` `047` `048`（网络输入与登录安全） | 这 14 条 P1 中 13 条仍存在：其中 `041`+`042` 决定"联机这一层能不能冻结"，`043`+`044` 决定本地进度会不会被联机污染 |
| **B. 再修（P2 确定性、协议与配置一致性）** | `013` `014` `015`（移动仿真）；`016` `017` `018` `023`（背包与存档）；`053` `054` `055` `056` `057`（协议与资源）；`002` `003` `004` `005` `064` `070`（架构与配置） | 都是"不改就会在联机 / 重开 / 换包场景下偶发"的问题，单独看每一条都能本地复现 |
| **C. 最后修（P3 生命周期与规范门禁）** | `006` `007` `008` `019`~`026` `030`~`040` `058`~`063` `065`~`069` `071`~`073` | 以"补门禁 + 清理"为主，量大但风险低；`064`~`069` 建议先落地 `.editorconfig` / 规则测试，再谈逐个改 |

性价比最高的"小改动、大收益"四条：`010`（体力恢复延迟改值 + 收紧测试）、`027`（AI 穿透力量纲）、`028`+`029`（入库失败回滚 + 带出价值重算）、`011`（物品状态绑实例，工作量是 L，但它决定弹药 / 护甲 / 存档三条线的语义）。

### 3. 必须实机确认的条目（共 7 条）

| 编号 | 要确认什么 | 判定手段 |
| --- | --- | --- |
| `041` | 联机客户端能否本地结束战局、服务端裁定是否被旁路 | 双客户端，客户端单边走到撤离读条结束 |
| `042` | 服务端是否永不产生超时结果 | 服务端日志 + 客户端挂机到战局时限 |
| `043` | 单机存档是否被联机污染（字节级） | 联机前后对 `save` 文件做哈希比对 |
| `044` | 联机使用医疗品的实际表现（是否回血 / 是否扣件 / 服务端是否知道） | 双客户端实测 + 服务端日志 |
| `047` | NaN / Infinity 输入能否污染权威位置与快照 | 修改过的测试客户端注入越界向量 |
| `048` | 未登录连接反复登录是否阻塞主循环 | 压测脚本打登录，观测 tick 抖动与 `accounts.json` 增长 |
| `012` / `016` / `023` | 弹挂容器广播覆盖度 / 跨网格实例唯一性 / 占用表损坏是否真发生 | 双客户端实测 + 定向构造 |

`041` 与 `043` 优先级最高：它们决定"联机这一层能不能先冻结"，其余可以并行推进。

## 三、边界声明

- 本片段只做**静态复核**：读代码 + `rg` 定位 + 对照 `Docs/` 与测试；未改任何受控源码、配置、场景或资源，未跑构建与测试。
- 未覆盖：真实双客户端时序、Scene / Prefab 序列化接线、Profiler / GC 量测、空 `Library` 干净包解析——对应上面的"必须实机确认"清单。
- 行号口径：均为 HEAD `7616783` 的真实行号；后续提交若改动行号，以编号问题为准。
