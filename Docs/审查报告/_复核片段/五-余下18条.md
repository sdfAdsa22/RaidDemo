# 第五节（AI / Raid / Meta / Presentation / UI / Bootstrap / Editor）余下 18 条复核

> 由 `verify_p1_core` 独立复验（只读读代码 + `rg` 定位，未改任何受控文件、未跑构建）。
> 复核时 HEAD = `7616783`。本片段只含 **RD-AUD-029 ~ 040、058 ~ 063** 这 18 行；
> `027` / `028` 已在主文档第五节末完成，不重复。行号均为复核时真实行号。
> 表格列与主文档一致，可直接粘进 `RaidDemo_审查问题状态复核.md` 第五节。

| 编号 | 原结论（≤20 字） | 现状 | 当前证据（文件:行号） | 说明 |
| --- | --- | --- | --- | --- |
| RD-AUD-029 | 入库前算带出价值 | **仍存在** | `Bootstrap/SceneBootstrap.RaidFlow.cs:349`、`:362`、`:373`；`Modules/Raid/RaidResult.cs:52`、`:99`；`Modules/Meta/MetaProgress.cs:248` | 结算数据先由 `RaidResult.Create` 按"携带物清单"算出 `ExtractedValue`，之后才 `DepositLoadoutToStash()`；入库失败只累加 `LastDepositFailures` 并打警告（`:363`-`:367`），结算价值与任务进度（`:373` 用 `result.ExtractedValue`）仍按"全部带出"计 |
| RD-AUD-030 | 调试视线用地表高度 0 | **仍存在** | `Modules/Diagnostics/AiDetectionDiagnostics.cs:159`；`Modules/AI/AISensor.cs:89`、`:91`；对照 `Modules/AI/AiAgent.cs:344` | 调试面板用两参 `ToEyePosition(observerPosition, profile.EyeHeightMeters)`，该重载把地面高度固定为 `0f`；真实 AI 用带 `eyeGround` 的三参版本 → 盆地 / 平台等多层地图上调试结论可能与 AI 实际视线不一致 |
| RD-AUD-031 | 医疗读条期间可移走物品 | **仍存在** | `Bootstrap/SceneBootstrap.ItemUse.cs:27`、`:33`、`:48`、`:54`-`:57`；`Bootstrap/SceneBootstrap.RaidFlow.cs:83`-`:86` | 完成后先写回血（`:27`）、再 `ConsumeOne`（`:33`）；`ConsumeOne` 在背包 / 弹药挂里找不到实例时直接 `return`（`:54`-`:57`）。读条只被"受伤"打断、期间允许移动，因此把物品移进战利品箱即可"回血不扣件" |
| RD-AUD-032 | 医疗消耗不发背包事件 | **仍存在** | `Bootstrap/SceneBootstrap.ItemUse.cs:58`、`:61`；`Modules/Data/Items/ItemInstance.cs:163`；`Modules/Inventory/InventoryGrid.Placement.cs:183`、`:191`；发布点仅 `Modules/Inventory/InventoryContext.cs:75`、`WeaponSwitchCommandHandler.cs:49`、`PlayerWeaponController.cs:327`、`LootContainerSync.cs:182`、`TraderCommandHandlers.cs:243` | 消耗走 `item.Split(1)` / `grid.Remove(item)`，这两条底层 API 都不发 `InventoryChangedEvent`；界面靠该事件刷新（`Modules/UI/Inventory/InventoryScreenController.cs:165`），所以已打开的背包不会即时刷新 |
| RD-AUD-033 | 领奖先加钱后放物品 | **仍存在** | `Modules/Meta/QuestSystem.Progress.cs:144`、`:152`、`:157`-`:161` | 已有 `CanAutoPlace` 前置检查（`:144`）与注释说明，但加钱（`:152`）与放入（`:157`）之间仍无回滚：`AutoPlace` 失败时只置 `problem` 并 `return false`，钱已到账 |
| RD-AUD-034 | 上交物品忽略放置结果 | **仍存在** | `Modules/Meta/QuestSystem.Progress.cs:315`、`:318` | 拆堆后 `m_Stash.Place(rest, origin, rotated)` 失败时兜底 `m_Stash.AutoPlace(rest);` 的返回值被丢弃，且随后 `remaining = 0` → 方法返回成功，占用表异常时静默丢件 |
| RD-AUD-035 | 过期记忆仍被当有效威胁 | **仍存在（判据分叉）** | `Modules/AI/AiContext.cs:51`（`HasFreshMemory` 用 `IsFresh`）、`:65`、`:73`（`TryResolveThreatPosition` 只判 `HasMemory`）；`Modules/AI/AISensesMemory.cs:75`、`:95`；生产清空仅 `Modules/AI/AiAgent.cs:320`；使用者 `States/RetreatState.cs:54`、`:77`、`EngageState.cs:71`、`:78`、`AlertState.cs:69`、`:92` | `DiscardIfExpired` 只有测试调用；记忆仅在"目标实体不存在"时被清空。`RetreatState.cs:54` 的注释写"看不见且记忆过期"，但 `TryResolveThreatPosition` 不判新鲜度 → 过期坐标仍会被用来算撤退方向；`EngageState` 反而先判 `HasFreshMemory`，两处口径不一致 |
| RD-AUD-036 | 伤害数字每次新建销毁 | **仍存在** | `Modules/Presentation/Combat/DamageNumberView.cs:73`、`:78`、`:88`、`:90`、`:121` | 每次命中 `new GameObject` + `AddComponent<TextMesh>`，由 `DamageNumberFade.Update` 到期 `Destroy(gameObject)`；生成时每帧路径还读 `Camera.main`（两次）。工程其它高频表现（弹道 / 枪口火焰）走对象池，此处是例外 |
| RD-AUD-037 | 运行时材质不释放 | **仍存在** | `Modules/Presentation/Player/StaminaArcView.cs:148`；`OnDestroy` 在 `:98`-`:101` | `CreateArcLine` 每条线 `new Material(Shader.Find("Sprites/Default"))`（两条线 → 两份），`OnDestroy` 只释放事件订阅，全文件无 `Destroy(material)`；反复进出战局会累积运行时材质 |
| RD-AUD-038 | 动画速度平滑与帧率相关 | **仍存在** | `Modules/Presentation/AI/EnemyAgentView.cs:117`、`:144`；`Modules/Presentation/AI/RemoteEnemyView.cs:37`、`:178`；驱动处 `Bootstrap/SceneBootstrap.Multiplayer.cs:257`、`:272`；对照 `Modules/Presentation/Player/PlayerMotor.cs:221`-`:222` | 本地敌人 `Update` 内 `Mathf.Lerp(m_SmoothedSpeed, speed, 0.35f)`、远端敌人同一常量 `SpeedSmoothing`，都是固定系数；`PlayerMotor` 已改成 `1 - exp(-rate*dt)` 的指数跟随，两套口径并存 |
| RD-AUD-039 | 调试关闭仍非零开销 | **仍存在** | `Modules/Diagnostics/AiDebugOverlay.cs:155`、`:157`、`:176`-`:192`；`Modules/Diagnostics/AiDebugPanel.cs:76`、`:89`、`:110`-`:115` | `Update` 每帧无条件 `HandleToggleKeys()`（读 F1/F2）；面板侧 `Refresh` 会在隐藏时早退，但 `OnStateChanged` 订阅不看可见性、仍格式化并插入日志 → 与 README"调试工具关闭时零开销"不一致 |
| RD-AUD-040 | 热路径仍有运行时 Find | **仍存在** | `Modules/Presentation/Player/PlayerCharacterView.cs:148`、`:153`-`:158` | `Update` 里 `m_WeaponView == null` 时依次 `GetComponent` → `FindFirstObjectByType<PlayerWeaponView>()`；武器视图由装配流程稍后创建，未创建期间的每一帧都会重复全场查找，与"运行时无 Find"规则冲突 |
| RD-AUD-058 | 事件订阅被丢弃不释放 | **仍存在** | `Modules/UI/Inventory/InventoryScreenController.cs:166`、`:294`-`:297`；`Bootstrap/SceneBootstrap.Combat.cs:88` | `EncumbranceChangedEvent` 的订阅返回值未保存（lambda），`OnDestroy` 只释放 `m_Subscription` → 该订阅无法注销，且 `Initialize` 被再次调用时会叠加；`SceneBootstrap.Combat` 的 `DamageAppliedEvent` 订阅同样丢弃返回值、无拆卸路径 |
| RD-AUD-059 | 约 20 处运行时 Find | **仍存在** | 热路径：`Bootstrap/SafeHouseBootstrap.Interaction.cs:31`、`:87`（+ `SafeHouseBootstrap.cs:315` 每帧调用）；其余代表点：`Modules/Presentation/Combat/DamageNumberView.cs:88`、`:90`、`Modules/UI/Raid/SafeHouseSignBoard.cs:185`-`:186`、`:210`、`Modules/Input/Runtime/PlayerInputCollector.Aiming.cs:35`、`Bootstrap/SafeHouseBootstrap.Combat.cs:250`、`Bootstrap/SceneBootstrap.Ai.cs:143`、`Bootstrap/SceneBootstrap.Raid.cs:185`、`:218`、`Bootstrap/Session/Network/ServerRuntime.Inventory.cs:86`、`ServerRuntime.Navigation.cs:130`、`ServerRuntime.Raid.Extraction.cs:30` | 统计口径：非 Tests / 非 Editor 代码里 `Find*` / `Camera.main` 调用点约 20 处（本次 `rg` 结果去掉注释后为 20 处），与报告吻合；其中只有安全屋交互是真热路径（每帧扫全场 `SafeHouseInteractable`），其余多为装配期一次性查找 |
| RD-AUD-060 | 关服不销毁幽灵碰撞体 | **仍存在** | `Bootstrap/Session/Network/ServerRuntime.Movement.cs:98`、`:115`-`:117`；对照正确路径 `ServerRuntime.Players.cs:88`-`:95`；失败分支 `ServerRuntime.Players.cs:44`、`:47`-`:51` | `ShutdownMovement` 只 `Clear()` 三个字典（`m_PlayerBodies` / `m_PlayerColliders` / `m_PlayerGroundHeights`），不销毁载体对象；`SpawnPlayerIntoWorld` 在 `TryAddPlayer` 失败时也只 `m_PlayerBodies.Remove(playerId)`。只有 `RemovePlayerFromWorld` 会 `Object.Destroy(body)` → 这两条路径可能留下可被射线命中的幽灵碰撞体 |
| RD-AUD-061 | 服务端带出价值漏装备槽 | **仍存在** | `Bootstrap/Session/Network/ServerRuntime.Raid.cs:249`（注释）、`:257`、`:266`-`:267` | `ResolveCarriedValue` 只累加 `loadout.Backpack` 与 `loadout.AmmoPouch`，方法注释自陈"装备槽留给 P5 的结算细化"；客户端 `Modules/Raid/RaidResult.cs` 的 `CollectCarriedItems` 是含装备槽的 → 两侧带出价值口径不一致 |
| RD-AUD-062 | Editor 工具先删后建 | **仍存在** | `Assets/Game/Bootstrap/Editor/UiAssetTool.cs:115`、`EnemyCharacterBuilder.cs:173`、`PlayerCharacterBuilder.cs:105`、`BasinTerrainMaterialBuilder.cs:57` | 四处仍是先 `AssetDatabase.DeleteAsset(...)` 再创建 / 导入；生成或导入中途异常时旧资产已删除，工具不幂等、也无法回滚 |
| RD-AUD-063 | 死代码与注释漂移 | **口径变化（部分已修）** | 已修：`Bootstrap/Session/Lobby/MultiplayerClientSession.Lobby.cs:241`（`ReturnFromRaid` 现有生产调用方，对应 `050`）；仍存在：`Bootstrap/SceneBootstrap.Multiplayer.Raid.cs:24`（`m_NextRestartRequestTime` 只有声明、全仓无读写）、`Modules/UI/Inventory/UiFontProvider.cs:9`（备注仍写"本项目尚未导入 TMP 的基础资源包"，而 `Modules/UI/Theme/UiFactory.cs:1` / `:132` 已用 `TMPro`）；已更新：`Bootstrap/SceneBootstrap.Inventory.cs:69` 注释改为"P5 起由共享容器链路接管" | 三条子结论里两条仍在（死字段一处、TMP 注释一处），`ReturnFromRaid` 那条已被 `050` 的修复消解，不再成立 |

## 本片段小计

| 现状 | 条数 | 编号 |
| --- | ---: | --- |
| 仍存在 | 17 | `029` `030` `031` `032` `033` `034` `035` `036` `037` `038` `039` `040` `058` `059` `060` `061` `062` |
| 口径变化 | 1 | `063` |
| 已修 | 0 | — |

## 对第七节结论的两条输入

1. **`029` 与 `028` 是同一个缺陷的两半**（入库失败无回滚 + 结算价值先于入库计算）：修 `028` 时应一并把 `ExtractedValue` / 任务进度改成"入库成功后重算"，否则仓库满时仍会出现"结算显示带出 8 万、仓库只多了 3 万"。
2. **`031` / `032` 是同一处代码的一对问题**（回血先于扣件 + 消耗不发事件）：`SceneBootstrap.ItemUse.cs` 的完成回调应改成"先把消耗做成 `InventoryContext` 链路里的原子操作，再结算回血"，两处一起修才不会再冒新的免费治疗路径。
