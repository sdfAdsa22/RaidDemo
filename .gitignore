# =============================================================================
# Unity 工程忽略规则
# =============================================================================
# 说明：本文件遵循 Unity 官方推荐模板，并按本工程需要做了注释。
# 核心原则——凡是由 Unity 或 IDE 自动生成、且内容含本机绝对路径的文件，一律不提交。

# -----------------------------------------------------------------------------
# Unity 自动生成的本地缓存目录（体积大，且含机器相关信息）
# -----------------------------------------------------------------------------
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/
[Mm]emoryCaptures/
[Rr]ecordings/

# Asset Store 下载的原始包（如需保留请在 README 中说明）
[Aa]ssets/AssetStoreTools*

# -----------------------------------------------------------------------------
# 自动生成的 IDE 工程文件
# -----------------------------------------------------------------------------
# .sln 与 .csproj 由 Unity 自动生成，内容包含本机绝对路径。
# 提交它们会导致他人克隆后 IDE 无法识别工程，属于必须忽略项。
*.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb

# Tools/ 下的 .csproj 与 .sln 是**手写**的工具工程（启动器 / 更新源服务端），不是 Unity 生成的，
# 必须入库——否则他人克隆仓库后这两个工具根本构建不了（这条曾真实踩到：工程文件被上面的 *.csproj 吃掉）。
!/Tools/**/*.csproj
!/Tools/**/*.sln
!/Tools/**/*.slnx
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# Rider / Visual Studio / VS Code
.idea/
.vs/
.vscode/

# -----------------------------------------------------------------------------
# 构建产物
# -----------------------------------------------------------------------------
*.apk
*.aab
*.unitypackage
*.app
Builds/
[Ee]xported[Oo]bj/

# .NET 工具（Tools/）的中间产物与发布输出；仅作用于 Tools 目录，避免误伤 Unity 侧同名目录
/Tools/**/bin/
/Tools/**/obj/
/Tools/**/publish/
/Tools/**/TestResults/
launcher.config.local.json

# Crash 报告
sysinfo.txt
crashlytics-build.properties

# -----------------------------------------------------------------------------
# 操作系统生成的文件
# -----------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# -----------------------------------------------------------------------------
# 本工程的额外忽略项
# -----------------------------------------------------------------------------
# 代码热更新产物（M10 阶段生成，属于构建输出而非源码）
[Aa]ssets/Game/Content/HotUpdateBundles/

# 本地热更包托管目录（仅用于本机演示热更流程）
[Tt]ools/LocalCdn/

# 本地构建脚本的输出
[Tt]ools/Out/

# 路径规则测试使用的临时目录（测试运行期间创建，结束后自动删除）
[Aa]ssets/**/TempPathCheck/

# -----------------------------------------------------------------------------
# M7 素材候选暂存区（重要）
# -----------------------------------------------------------------------------
# 规则：第三方素材先放进 External/_Inbox/ 里做导入与观感验证，此时**不入库**；
# 只有负责人确认采用的最终版本，才从 _Inbox 移到 External/<来源>/<资源包>/ 并提交。
# 好处：Unity 能正常导入预览，同时 Git 里永远不会出现"试用后淘汰"的素材及其历史。
[Aa]ssets/Game/Content/External/_Inbox/
[Aa]ssets/Game/Content/External/_Inbox.meta

# 本机部署备忘（含云主机地址等接入信息，不入库）
[Dd]ocs/Local/

# 测试框架在构建时写入的性能采样产物（本工程资源一律放 Assets/Game/Content）
[Aa]ssets/[Rr]esources/
[Aa]ssets/[Rr]esources.meta

# HybridCLR 探针产物（M10 批次 5 探针；探针未通过，工程已退回 Mono，目录不入库）
HybridCLRData/

# HybridCLR 生成物（探针遗留；不含 HybridCLR 类型，留盘但入库无意义）
/Assets/HybridCLRGenerate/
/Assets/HybridCLRGenerate.meta
